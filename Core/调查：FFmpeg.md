# 调查：FFmpeg

## 库

libavcodec : 编码、解码库

libavfilter：基于图的帧编辑库

libavformat: 特殊的设备的复用解复用库

libavutli： 普通工具库

libswscale：颜色转换和缩放库

libswresample：音频采样，格式转换和混合

> 只要主版本号不变，FFmpeg 就保证每个库的 API 和 [ABI](https://ffmpeg.org/doxygen/trunk/namespaceABI.html) 向后兼容。

