[TOC]



### StringUtils

- StartsWith and Endwith

  if else

  compare(o,noa sub)

- FilePath

- get filepath name and suffix

-  vector<.string> string split(string , delimiter);

  > what is  返回值优化? 没有开销是什么？

### xxx

控制一个实例；单例模式

noncopyable

控制单次调用：int pthread _once(value , func*)

- 私有化构造和析构，转用static init
- 使用模版
- 静态函数类外初始化

**定时任务**：在一个时间点或时间间隔内执行任务 

函数回调+智能指针

> question: enable_share_fron_this



上层调用：定时任务管理器，负责：

- 检测任务时间，执行定时任务
- 管理所有定时任务

功能包括；

- 定时任务的遍历（即到点执行）
- 添加和删除
- 。。。



设计（DSA）

- 直接遍历

  遍历，有就执行，更新，循环

  ds vector	

- 顶堆

  heap: min在root 所以小于他的值忽略。 使用一个任务队列管理任务

  ds heap vector queue 

  

- 时钟  大小钟没听明白

> on_xx ： 当xx发生时，执行内容的一种韩式命名方式

这里选择 直接遍历

- 成员变量：定时任务的管理，锁
- 成员函数：onWork：启动任务   
                    addTask：添加任务 delTask 删除任务 （传入任务)

添加和删除：

- 启用lock_guard添加锁

- 获取任务的位置，没有就返回 false ，有就向manager添加，并返回true

  > 为什么要返回 true false , 又不会被别的函数调用判断?

- 删除：直接 erase

on_work:

​	获取当前时间，计算差值，比较interval《 当前时间，执行任务

​	回调有没有更新时间 ，没有就删除掉？



日志系统： 从实现控制台，到实现文件

- 查问题 debug
- 输出业务信息  负载

组成：

​	logStream : 管理输出

​	logger: 输出日志

​		filelog: 输出到文本，对文本进行额外操作的



logger: 

> eg: [debug] [time] [thread_id] "text"

日志级别：设置最小日志界别，避免无效输出（如过用作调试，那我不希望看到一半的信息）

时间：获取当前精确的时间

格式化：字面意思

**logStream:**

成员变量；logger， log_level , 

set:

formatter(logger, file level func_name) 

使用宏简化命令输出，在宏中判断日志等级(if-else),负责不同的输出.

**日志库的实现**：

> 切分：根据文件大小或时间，自动创建新文件。定期清理旧文件。以防单个文件过大，难以查询。

关键：

- 文件操作要封装：文件的打开，写入，切分等。

- 架构：

  **Logger 决定“写什么”**
   **Sink 决定“写到哪里”**
   **FileAdapter 决定“怎么写到操作系统”**

- 打开文件 (include <.sys/fcntl>)  open(file_path, mode)

  mode: O_APPEND O_CREATE O_RWONLY etc...

  写入字节 write   *fd==-1?1:fd_*  

  刷盘 flush ：将内存 中的数据持久化到硬盘中去

  获取文件大小 lseek64(fd_,0,SEEK_END)

  重命名

  关闭文件

  切分: 切换日志文件

  ​	切分类型：按小时，按天etc

  ​	打开文件（获得fd）dup2(fd,fd_) 复制fd

  ​	补充，多线程运行日志系统，write + O_APPEND = 原子追加 。 可以判断一下如果dup操作失败后，关闭fd

file-Manager

功能：

​	按小时、天 切分	

​	创建日志

实现:

​	onCheck：设置时间（判断是否满足 hour or day) ,开始写切分

​	getFileLog

​	rotateDay

​	rotateHour

​	

#### extra

## 回源

DTS decode timestamp

PTS presentation timestamp



客户端向一个服务器请求某个流时，服务器有这个流就马上返回数据；服务器没有这个流，就需要去中心服务器请求这个流的数据，这就是回源。

回源可能是多级的，也可以是多协议支持的。



## 时移

在直播过程中，可能需要查看前面一段时间的数据，或者从前面几秒开始查看直播，这个就是时移做的功能。

时移是通过录制产生的切片信息实现的，所以时移使用是的HLS协议。

时移一般有两种播放方式：

- 播放过去的一段时间
- 从过去的哪个时间点一直往后播放，直到结束

## 转码

在拉流/播放过程中，服务器遇到不支持的编码请求或者不同的分辨率，或者其他情况，就会触发转码。

转码就是通过程序，把不符合要求的流，转码成符合要求的流。

转码一般使用ffmpeg进行，同时也可以使用显卡进行硬件加速。



## CDN分发

主播推流，一般只推送到一个服务器，而播放却是在全国各地，这就需要CDN把流传输到各地的服务器，这就是CDN分发。

CDN分发一般是被动的，有访问需求才进行回源；也可以主动下发进行预热。

## CDN加速

客户端的网络环境差异很大，为了优化播放端的体验，CDN需要把播放引导到离客户最近的，网速最快的服务器，相当于CDN给客户端提升了速度，这就是CDN加速。

CDN加速主要是通过减少访问服务器的距离，提供大带宽的服务，选择相同的运营商，选择延时低的流媒体协议等手段来实现内容的加速。



# 一个直播的整个流程及背后的故事

一个主播启动直播，到用户进行播放，这中间发生过什么事？为什么需要直播分发和直播加速？

这节课我们就来聊聊这个过程及相关的技术。

## 推流端

主播通过直播APP主动发起直播请求，直播APP启动音视频采集，然后采集的数据编码成H64/AAC，接下来封装成推流协议的数据格式，把数据推送到边缘服务器。

1. 采集

一般通过手机摄像头或者采集卡采集到视频数据，原始数据是一幅幅的图片，通过硬件编码，编码成H264/H265格式的视频数据。

音频是通过麦克风采集，通过硬件编码，编码成AAC格式的音频数据。

1. 推流协议封装

直播APP根据使用的推流协议，把编码后的视频数据和音频数据封装成推流协议需要的格式

1. 推流

直播APP通过推流域名获取到最优的推流服务器，使用推流协议连接上推流服务器，把封装好的数据推送到服务器。

一般云厂商会做DNS负载调度，根据访问DNS服务器的IP，选择相同运营商，距离最近，负载最轻的服务器。

选择合适的推流协议，选择合适的推流服务器，都是为了解决`第一公里`的问题。

## 推流服务器

1. 解封装

推流服务器根据推流协议，跟直播APP建立连接，接收推送过来的数据，并且解开封装，得到音频和视频数据，以及Meta信息。

1. 通知索引服务器

推流服务器接收到一路推流后，会通知索引服务器，并带上流的各种信息。

索引服务器收到通知后，会记录这种流，以便其他服务器过来查寻时，能够返回正确的推流服务器地址。

索引服务器也可以通知业务服务器，新增加了一路直播流，直播APP就可以刷新看到这路新的流。

## 播放端

1. 播放

直播APP刷新后，出现了新推上来的直播流，点击播放，直播APP通过直播流的域名，获取到最优的播放服务器。

直播APP根据获取到的播放服务器地址，使用播放协议（HLS/RTMP/HTTP-FLV）建立连接，接收数据。

播放器接收到数据后，进行解封装，解编码格式，渲染输出画面，用户就能看到界面

1. 最后一公里

`最后一公里`是指播放服务器到用户端的过程。

直播APP根据播放域名，从DNS服务器获取最优的播放服务器地址（也有可能是通过访问负载均衡服务器）进行播放；而DNS服务器（或者负载均衡服务器）需要从运营商，IP距离，负载情况等多方面考虑，选择最优的一个服务器。

同时，直播APP要根据业务需求，用户网络环境等选择合适的播放协议进行播放。

`最后一公里`需要直播系统和直播APP，业务设计等多方面共同参与优化，给用户最好的体验。

## CDN

1. 回源

当直播APP向边缘节点（DNS返回的播放服务器）请求这个直播流时，边缘节点会先检测本地有没有这个流；如果没有找到，则需要向回源服务器请求数据。

边缘节点接收到回源数据后，马上会返回给直播APP，这样用户就可以及时看到画面。

1. 中转

边缘节点的运营商，可能跟推流服务器的运营商不一样，这时候就需要中转服务器来处理。

首先，中转服务器都是多线服务器，即中转服务器有多个网卡，每个网卡接一个运营商。

其次，边缘节点向中转服务器请求，是从边缘节点相同的运营商的线路发起请求。

最后，中转服务器推流服务器拉流时，是从推流服务器相同的运营商线路发起拉流请求。

所以，通过中转服务器处理后，边缘节点和推流服务器的运营商差异就被屏蔽了，用户是无感知的。

## 总结

一个直播，从终端到CDN，再从CDN到终端，每个环节都涉及很多技术，都是必不可少的。

直播系统在这个过程中，提供了推流功能，CDN分发，CDN加速功能，以及播放功能。

如果主播还需要录视频，CDN还需要启动录制功能。

直播平台需要对内容进行审查，则需要时移及其他定制的功能。

用户播放这个主播的流，手机屏幕不一样，手机芯片不一样，可能需要不一样的分辨率，不一样的编码格式，这时候直播CDN就需要为这个流提供转码的能力。

直播系统是一个庞大的分布式系统，虽然功能很多，很复杂，但是都是推流，拉流的基础上扩展的。所以，接下来的章节，我们首先会实现推流，然后再实现拉流，最后扩展出其他的功能。



### net layer

prepare:

​	C/S  vs P2P

​	CPU vs IO

正确的认识网络服务器的分类，有助于在开发阶段帮助我们选择网络架构，选择硬件软件配置

了解服务器所需要的资源，可以在设计阶段进行意向针对性的优化和部署

​	服务器性能指标

- 吞吐量，
- 延时
- 和资源使用率。

吞吐量：一个系统每秒钟处理业务的能力

eg QPS 请求（衡量短连接为主的业务系统

​     并发数：系统同时处理请求的最大数 （长连接

​	数据量：字节数（很亮处理文本

系统完成后，通过压力测试得到的数据

延时：从请求开始到收到响应所花费的总体时间

影响延时的因素，大概有几种：

- DNS查询，根据本地DNS和全局DNS，为了降低延迟，可以刷新DNS，或者直接使用IP，来减少解析时间的目的
- TCP握手，
- TCP传输
- 业务处理能力：主要可以优化的部分

在保证低延迟的基础上，再考虑高吞吐

​	资源使用率

CPU，内存，硬盘，网络

文件描述符，虚拟内存，进程线程。每一种资源都有对应的工具可以查看。在系统运行时，我们要经常关注资源的使用率，不断的优化性能，以更少的资源，实现性能更好的服务。

从系统的角度看，吞吐量是最重要的指标，能直观的反映系统的性能。
从用户的角度看，延时是最重要的指标，能直接感受系统性能。
从操作系统的角度看，资源使用率是衡量一个系统程序性能最有效的方法。



#### IO模型

IO模型是指，IO操作常用的处理方式，编程模型

主要分为两种，同步IO和异步IO

区别在于 IO的读写操作都是由：应用程序 or 内核 完成

同步之下，又有阻塞，非阻塞，IO多路复用

阻塞：当用户进程发出IO请求后，内核去查看数据是否就绪，没有就等着，知道OK就再返回。

​	特点：CPU 大部分时间再阻塞，而不能做其他事。只适合简单的业务，如DNS，。

非阻塞：没有等待，马上返回结果 ，需要轮询内核是否就绪，导致CPU占用率非常高，时间复杂，编程困难。不推荐

多路复用：内核同时监控多个文件描述符，由内核负责返回就绪的文件描述符，供用户处理，减少了系统调用和轮询的开销，提高了系统的并发性，但是epoll系统调用和拷贝阶段时，依旧是阻塞的。

异步IO

内核来完成IO的读写，IO读写事件处理完后，才通过回调通知用户：

内核在数据拷贝完成时向它发送一个信号或者执行一个回调函数。实现复杂，并不是所有OS支持

**总结：**

1. 同步IO模型中的模型都会阻塞：

- 阻塞IO模型在系统调用会阻塞
- 非阻塞IO模型在读写数据时会阻塞
- IO复用模型在多路复用函数调用时会阻塞，在IO读写数据时会阻塞

1. 异步IO模型的程序逻辑不符合人的思维习惯，比较难阅读理解
2. 在IO密集型程序中，不管是同步IO，还是异步IO都不能提升整体的吞吐量
3. 在流媒体服务器中，网络IO是主要的瓶颈，只要能把网卡吞吐量打满，不管用同步还是异步IO都可以实现



#### 两种高效的事件处理模式

> - Proactor依赖系统提供的AIO，但是linux提供的异步IO还不完善。目前使用的AIO一般是用户空间模拟的异步，并不是真正的异步。
> - Proactor的程序逻辑比较离散，阅读理解比较困难
> - Reactor的性能已经满足要求，并且代码逻辑清晰，容易理解
> - 目前大部分服务器还是以Reactor模式为主

分析后续补充



网络编程 API

后续补充



